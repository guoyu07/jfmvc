<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.hyts.jfmvc.app</groupId>
  <artifactId>hyts-application</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>war</packaging>
  <properties>
  	    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    	<maven-compiler.plugin.version>3.6.0</maven-compiler.plugin.version>
    	<maven-surefire.plugin.version>2.19.1</maven-surefire.plugin.version>
    	<jdk.version>1.8</jdk.version>
    	<junit.version>3.8.1</junit.version>
    	<jfmvc.version>1.0.1</jfmvc.version>
    	<!-- jetty-server服务启动配置 -->
    	<jetty.version>9.3.7.v20160115</jetty.version>
    	<jetty.contextPath>/hyts</jetty.contextPath>
    	<jetty.address>localhost</jetty.address>
    	<jetty.start.port>9090</jetty.start.port>
    	<jetty.stop.port>9966</jetty.stop.port>
  </properties>
  <dependencies>
  	<dependency>
		<groupId>com.hyts.jfmvc</groupId>
		<artifactId>hyts-jfmvc-web</artifactId>
		<version>${jfmvc.version}</version>
    </dependency>
    <dependency>
        <groupId>junit</groupId>
        <artifactId>junit</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${maven-compiler.plugin.version}</version>
				<configuration>
					<source>${jdk.version}</source>
					<target>${jdk.version}</target>
					<encoding>${project.build.sourceEncoding}</encoding>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${maven-surefire.plugin.version}</version>
				<configuration>
					<skip>true</skip>
				</configuration>
				
			</plugin>
			<plugin>
  				<groupId>org.eclipse.jetty</groupId>
  				<artifactId>jetty-maven-plugin</artifactId>
  				<version>${jetty.version}</version>
  				<configuration>
  					<connectors>
           				<connector implementation="org.eclipse.jetty.server.nio.SelectChannelConnector">
             				<port>${jetty.start.port}</port>
             				<maxIdleTime>60000</maxIdleTime>
          				</connector>
         			</connectors> 
  					<webApp>  
     					<contextPath>${jetty.contextPath}</contextPath>  
  					</webApp>
    				<httpConnector>
        				<port>${jetty.start.port}</port>
        				<host>${jetty.address}</host>
    				</httpConnector>
    				<scanIntervalSeconds>3</scanIntervalSeconds>
    				<stopKey>shutdown</stopKey>
  					<stopPort>${jetty.stop.port}</stopPort>
  					<requestLog implementation="org.eclipse.jetty.server.NCSARequestLog"> 
       					<filename>target/access-yyyy_mm_dd.log</filename>        <filenameDateFormat>yyyy_MM_dd</filenameDateFormat>  
        				<logDateFormat>yyyy-MM-dd HH:mm:ss</logDateFormat>  
        				<logTimeZone>GMT+8:00</logTimeZone>  
        				<append>true</append>  
        				<logServer>true</logServer>  
        				<retainDays>120</retainDays>  
        				<logCookies>true</logCookies>  
        			</requestLog>  
				</configuration>
			</plugin>
		</plugins>
  </build>
</project>